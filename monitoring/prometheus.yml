global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alertmanager.yml

scrape_configs:
  - job_name: infrawatch-app
    static_configs:
      - targets: ["app:8000"]
    metrics_path: /metrics
